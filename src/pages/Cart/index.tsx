import React from 'react';
import {SafeAreaView, Text} from 'react-native'

const Cart:React.FC = () => {
    return(
    <SafeAreaView>
        <Text>Carrinho</Text>
    </SafeAreaView>
    )
}

export default Cart;